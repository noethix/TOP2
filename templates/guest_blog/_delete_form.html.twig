<form method="post" action="{{ path('guest_blog_delete', {'id': guest_blog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guest_blog.id) }}">
    <button class="btn">Delete</button>
</form>
