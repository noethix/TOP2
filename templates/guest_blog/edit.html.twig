{% extends 'base.html.twig' %}

{% block title %}Edit GuestBlog{% endblock %}

{% block body %}
    <h1>Edit GuestBlog</h1>

    {{ include('guest_blog/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('guest_blog_index') }}">back to list</a>

    {{ include('guest_blog/_delete_form.html.twig') }}
{% endblock %}
